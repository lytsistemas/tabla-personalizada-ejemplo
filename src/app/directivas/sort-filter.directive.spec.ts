import { SortFilterDirective } from './sort-filter.directive';

describe('SortFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new SortFilterDirective();
    expect(directive).toBeTruthy();
  });
});
